import React from "react";
import classes from './Footer.module.css';
const Footer = () => (
  <div className={classes.footer}>
    <button className={classes.youtube_icon}></button>
    <button></button>
  </div>
  
);

export default Footer;